# Carepoint Health App - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        MOBILE APP (React Native)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Screens    │  │  Components  │  │   Contexts   │          │
│  │              │  │              │  │              │          │
│  │ • Profile    │  │ • UI Kit     │  │ • Auth       │          │
│  │ • Meds       │  │ • Forms      │  │ • Theme      │          │
│  │ • Records    │  │ • Lists      │  │ • User       │          │
│  │ • History    │  │ • Modals     │  │              │          │
│  └──────┬───────┘  └──────────────┘  └──────────────┘          │
│         │                                                         │
│         │ Uses tRPC Hooks                                        │
│         ▼                                                         │
│  ┌─────────────────────────────────────────────────────┐        │
│  │              tRPC Client (lib/trpc.ts)              │        │
│  │  • Type-safe API calls                              │        │
│  │  • Automatic auth token injection                   │        │
│  │  • React Query integration                          │        │
│  └─────────────────────┬───────────────────────────────┘        │
│                        │                                         │
└────────────────────────┼─────────────────────────────────────────┘
                         │ HTTPS + JWT Token
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                    BACKEND (Hono + tRPC)                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │           API Router (backend/trpc/app-router.ts)  │         │
│  │                                                     │         │
│  │  • profile.*        - User profile endpoints       │         │
│  │  • medications.*    - Medication CRUD              │         │
│  │  • healthRecords.*  - Health records management    │         │
│  │  • emergencyContacts.* - Emergency contacts        │         │
│  │  • medicalHistory.* - Medical history              │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                          │
│                       │ Validates JWT & Extracts User            │
│                       ▼                                          │
│  ┌────────────────────────────────────────────────────┐         │
│  │      Context (backend/trpc/create-context.ts)      │         │
│  │  • Validates authentication                        │         │
│  │  • Provides ctx.user to procedures                 │         │
│  │  • Protected vs Public procedures                  │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                          │
└───────────────────────┼──────────────────────────────────────────┘
                        │ SQL Queries
                        │
┌───────────────────────▼──────────────────────────────────────────┐
│                    SUPABASE (Database + Auth)                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │              PostgreSQL Database                     │        │
│  │                                                      │        │
│  │  Tables:                                             │        │
│  │  • profiles              - User profiles             │        │
│  │  • medications           - Medication tracking       │        │
│  │  • health_records        - Medical documents         │        │
│  │  • medical_conditions    - Conditions history        │        │
│  │  • surgeries             - Surgery history           │        │
│  │  • allergies             - Allergy information       │        │
│  │  • emergency_contacts    - Emergency contacts        │        │
│  │  • health_tracker_data   - Daily health metrics      │        │
│  │  • connected_devices     - Device integrations       │        │
│  │  • appointments          - Doctor appointments       │        │
│  │  • symptom_logs          - Symptom tracking          │        │
│  │  • user_preferences      - App settings              │        │
│  │  • audit_logs            - HIPAA compliance          │        │
│  │                                                      │        │
│  │  Security:                                           │        │
│  │  ✅ Row Level Security (RLS) on all tables          │        │
│  │  ✅ Users can only access their own data            │        │
│  │  ✅ Encrypted at rest                               │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │              Supabase Auth                           │        │
│  │                                                      │        │
│  │  • Email/Password authentication                     │        │
│  │  • Google OAuth (optional)                           │        │
│  │  • JWT token generation                              │        │
│  │  • Session management                                │        │
│  │  • Email verification                                │        │
│  │  • Password reset                                    │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │              Supabase Storage                        │        │
│  │                                                      │        │
│  │  Buckets:                                            │        │
│  │  • health-records (Private)  - Medical documents     │        │
│  │  • profile-avatars (Public)  - User avatars          │        │
│  │                                                      │        │
│  │  Security:                                           │        │
│  │  ✅ User-specific folders                           │        │
│  │  ✅ File size limits                                │        │
│  │  ✅ MIME type restrictions                          │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│   User       │
│   Opens App  │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Check if user is authenticated      │
│  (supabase.auth.getSession())        │
└──────┬───────────────────────────────┘
       │
       ├─── Not Authenticated ──────────────────┐
       │                                        │
       │                                        ▼
       │                              ┌─────────────────┐
       │                              │  Show Auth      │
       │                              │  Screens        │
       │                              │  (Sign In/Up)   │
       │                              └────────┬────────┘
       │                                       │
       │                                       │ User signs in
       │                                       ▼
       │                              ┌─────────────────┐
       │                              │  Supabase Auth  │
       │                              │  Validates      │
       │                              │  Credentials    │
       │                              └────────┬────────┘
       │                                       │
       │                                       │ Returns JWT
       │                                       ▼
       │                              ┌─────────────────┐
       │                              │  Store Session  │
       │                              │  in AsyncStorage│
       │                              └────────┬────────┘
       │                                       │
       └─── Authenticated ─────────────────────┘
                                               │
                                               ▼
                                     ┌─────────────────┐
                                     │  Show App       │
                                     │  Screens        │
                                     │  (Dashboard)    │
                                     └─────────────────┘
```

---

## 📡 API Request Flow

```
┌──────────────┐
│  Component   │
│  Calls API   │
└──────┬───────┘
       │
       │ trpc.medications.getAll.useQuery()
       ▼
┌──────────────────────────────────────┐
│  tRPC Client (lib/trpc.ts)           │
│  • Gets session token                │
│  • Adds Authorization header         │
│  • Serializes request with superjson │
└──────┬───────────────────────────────┘
       │
       │ POST /api/trpc/medications.getAll
       │ Authorization: Bearer <jwt-token>
       ▼
┌──────────────────────────────────────┐
│  Backend (backend/hono.ts)           │
│  • Receives request                  │
│  • Routes to tRPC handler            │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Context (create-context.ts)         │
│  • Extracts JWT from header          │
│  • Validates token with Supabase     │
│  • Gets user from token              │
│  • Provides ctx.user                 │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Procedure (medications/get-all)     │
│  • Checks if user is authenticated   │
│  • Queries database                  │
│  • Filters by user_id                │
└──────┬───────────────────────────────┘
       │
       │ SELECT * FROM medications WHERE user_id = ?
       ▼
┌──────────────────────────────────────┐
│  Supabase Database                   │
│  • RLS checks user_id matches        │
│  • Returns matching rows             │
└──────┬───────────────────────────────┘
       │
       │ Returns data
       ▼
┌──────────────────────────────────────┐
│  tRPC Client                         │
│  • Deserializes response             │
│  • Caches in React Query             │
│  • Updates component                 │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────┐
│  Component   │
│  Renders     │
│  Data        │
└──────────────┘
```

---

## 🗂️ File Structure

```
carepoint-health/
│
├── app/                          # Expo Router screens
│   ├── (tabs)/                   # Tab navigation
│   ├── index.tsx                 # Entry point
│   ├── profile.tsx               # Profile screen
│   ├── my-medication.tsx         # Medications screen
│   └── ...
│
├── components/                   # React components
│   ├── auth/                     # Auth components
│   ├── ui/                       # UI components
│   └── ...
│
├── contexts/                     # React contexts
│   ├── AuthThemeContext.tsx      # Auth + Theme
│   ├── MedicationContext.tsx     # Medications (to be replaced)
│   └── ...
│
├── lib/                          # Client libraries
│   ├── supabase.ts              # Supabase client
│   └── trpc.ts                  # tRPC client
│
├── backend/                      # Backend code
│   ├── hono.ts                  # API server
│   ├── trpc/
│   │   ├── app-router.ts        # Main router
│   │   ├── create-context.ts    # Auth context
│   │   └── routes/              # API endpoints
│   │       ├── profile/
│   │       ├── medications/
│   │       ├── health-records/
│   │       ├── emergency-contacts/
│   │       └── medical-history/
│   ├── database/
│   │   ├── schema.sql           # Database schema
│   │   └── types.ts             # TypeScript types
│   └── lib/
│       └── supabase-server.ts   # Server Supabase client
│
├── constants/                    # App constants
├── assets/                       # Images, fonts
│
├── .env                         # Environment variables
├── package.json                 # Dependencies
└── tsconfig.json                # TypeScript config
```

---

## 🔄 Data Flow Example: Adding a Medication

```
1. User fills form in AddMedicationScreen
   ↓
2. User taps "Save" button
   ↓
3. Component calls:
   addMedication.mutate({
     name: 'Aspirin',
     dosage: '100mg',
     frequency: 'daily',
     time: ['09:00'],
     start_date: '2025-01-01'
   })
   ↓
4. tRPC client:
   • Gets JWT token from Supabase session
   • Sends POST to /api/trpc/medications.add
   • Includes Authorization header
   ↓
5. Backend receives request:
   • Validates JWT token
   • Extracts user from token
   • Calls addMedicationProcedure
   ↓
6. Procedure:
   • Validates input with Zod schema
   • Adds user_id to data
   • Inserts into medications table
   ↓
7. Supabase:
   • RLS checks user is authenticated
   • Inserts row
   • Returns new medication
   ↓
8. Backend returns response
   ↓
9. tRPC client:
   • Receives response
   • Calls onSuccess callback
   • Invalidates medications.getAll query
   ↓
10. React Query:
    • Refetches medications list
    • Updates UI automatically
    ↓
11. User sees new medication in list
```

---

## 🛡️ Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Network Security                              │
│  ✅ HTTPS encryption for all requests                   │
│  ✅ JWT tokens for authentication                       │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: API Security                                  │
│  ✅ tRPC validates all inputs with Zod                  │
│  ✅ Protected procedures require authentication         │
│  ✅ Type-safe API prevents injection attacks            │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Database Security                             │
│  ✅ Row Level Security (RLS) on all tables              │
│  ✅ Users can only access their own data                │
│  ✅ Encrypted at rest                                   │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Audit & Compliance                            │
│  ✅ All data access logged                              │
│  ✅ HIPAA-compliant audit trail                         │
│  ✅ Point-in-Time Recovery enabled                      │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Technology Stack Summary

| Layer | Technology | Purpose |
|-------|-----------|---------|
| **Frontend** | React Native + Expo | Cross-platform mobile app |
| **Routing** | Expo Router | File-based navigation |
| **State** | React Query + Contexts | Data fetching & local state |
| **API** | tRPC | Type-safe API layer |
| **Backend** | Hono | Lightweight Node.js server |
| **Database** | Supabase (PostgreSQL) | Relational database |
| **Auth** | Supabase Auth | User authentication |
| **Storage** | Supabase Storage | File storage |
| **Types** | TypeScript | Type safety |
| **Validation** | Zod | Schema validation |

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Production Setup                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Mobile App                                              │
│  ├── iOS App Store                                       │
│  ├── Google Play Store                                   │
│  └── Web (Progressive Web App)                           │
│                                                          │
│  Backend API                                             │
│  └── Hosted by Rork (automatically deployed)             │
│                                                          │
│  Database & Auth                                         │
│  └── Supabase Cloud (managed PostgreSQL)                 │
│                                                          │
│  File Storage                                            │
│  └── Supabase Storage (S3-compatible)                    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability

The architecture is designed to scale:

- **Database**: Supabase can handle millions of users
- **API**: Stateless backend can scale horizontally
- **Storage**: S3-compatible storage scales automatically
- **Caching**: React Query caches data on client
- **CDN**: Static assets served via CDN

---

## 🔍 Monitoring & Observability

```
┌─────────────────────────────────────────────────────────┐
│  Supabase Dashboard                                      │
│  • Database metrics                                      │
│  • API usage                                             │
│  • Storage usage                                         │
│  • Auth metrics                                          │
│  • Slow query logs                                       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Application Logs                                        │
│  • Console logs in development                           │
│  • Error tracking (Sentry recommended)                   │
│  • Analytics (optional)                                  │
└─────────────────────────────────────────────────────────┘
```

---

*This architecture provides a solid foundation for a production-ready health app with HIPAA compliance, type safety, and excellent developer experience.*
